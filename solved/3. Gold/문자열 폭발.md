```c++
#include <iostream>
#include <string>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	
	string s;
	string bomb;
	string remain;
	cin >> s >> bomb;

	const int length = bomb.length();
	
	for (char c : s)
	{
		remain += c;

		if (remain.size() >= length && remain.substr(remain.size() - length, remain.size()) == bomb)
		{
			string ss = remain.substr(remain.size() - length, remain.size());
			remain.erase(remain.size() - length, remain.size());
		}
	}
		
	if (remain.empty())
		cout << "FRULA";
	else
		cout << remain;
}
```
```
s = "misC4ticCC44"
bomb = "C4"

-> mis(C4)
-> mis
-> misticC(C4)
-> misticC
-> mistic(C4)
-> mistic
```
### [9935] 문자열 폭발
1. 남은 문자열중 해당하는 변수가 있는지 체크하고 계속 제거하는게 핵심이다.
2. 기존 ```Find()``` 함수의 반복은 많은 리소스를 잡아먹기에 ```substr()```함수를 통해 계속 비교하여 해당문자열 찾았다.


 